'use client'

import React from 'react'

// Client-side component to render the countdown
const ClientCountdownDisplay = () => {
  // Return null to hide the countdown
  return null;
}

export default ClientCountdownDisplay 